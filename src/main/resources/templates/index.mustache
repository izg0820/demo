{{>layout/header}}

<p></p>
<div class="container">
<!--    <div class="row gx-4">-->
<!--        <div class="col-1">-->
<!--            <label class="form-label fw-bold">고정확장자</label>-->
<!--        </div>-->
<!--        <div class="col-6">-->
<!--            <div th:each="fixedFileExtension, idx : ${fixedFileExtensionList}" class="form-check form-check-inline">-->
<!--                <form id="updateFixedExtension" th:action="@{'/extension/'+${fixedFileExtension.extension}}"-->
<!--                      th:object="${fileExtensionRequestDto}" method="post">-->
<!--                    <input type="hidden" name="_method" value="PUT"/>-->
<!--                    <input class="form-check-input" type="checkbox"-->
<!--                           th:id="${fixedFileExtension.extension}"-->
<!--                           th:value="${fixedFileExtension.isUse}"-->
<!--                           th:checked="${fixedFileExtension.isUse eq true}"-->
<!--                           th:onchange="updateFixedOrNot([[${fixedFileExtension.extension}]])">-->
<!--                    <label th:for="${fixedFileExtension.extension}" class="form-check-label"-->
<!--                           th:text="${fixedFileExtension.extension}"></label>-->
<!--                    <button th:id="'update_'+ ${fixedFileExtension.extension}" style="display: none"></button>-->
<!--                </form>-->
<!--            </div>-->
<!--        </div>-->
<!--    </div>-->
    <div class="row gx-4">
        <div class="col-1">
            <label class="form-label fw-bold">커스텀확장자</label>
        </div>
        <div class="col-8">
            <form class="row row-cols-2">
                <div class="col-6">
                    <input class="form-control" type="text" id="custom_extension" placeholder='확장자 입력'/>
                </div>
                <div class="col-2">
                    <button id="btn_custom_save" class="btn btn-outline-success" type="button"> +추가</button>
                </div>
            </form>
        </div>
    </div>
    <div class="row gx-4">
        <div class="card col-6" style="margin-top : 10px">
            <div class="card-body" id="card">
                <div> {{customFileExtensionSize}} / 200</div>
                {{#customFileExtensionList}}
                <div class="badge bg-success bg-primary">
                    <label> {{extension}} </label>
                    <form id="deleteCustomExtension">
                        <button id="delete-btn" class="btn-close" aria-label="Close"></button>
                    </form>
                </div>
                {{/customFileExtensionList}}
            </div>
        </div>
    </div>
</div>

{{>layout/footer}}